import StaticPageLayout from '../components/StaticPageLayout';

export default function TermsPage() {
  return (
    <StaticPageLayout title="Terms">
      <p>
        Do Different helps users discover experiences and redirects them to providers.
        Bookings, payments and participation terms are handled directly by each provider.
      </p>
    </StaticPageLayout>
  );
}
